FLL_DESCRIPTION="Java - installs openjdk-7 and icedtea."

FLL_PRE_PROCESSING=(
	"#!/bin/bash"
        "echo \"---- in preprocessing ----\""
        "apt-get update"
	"# paste your code here (not necessary for normal install, only for preconfigs)"
        "echo \"---- preprocessing done----\""
)

FLL_PACKAGE_DEPMODS=(
)

FLL_PACKAGES=(
  
      openjdk-7-jre
      openjdk-7-jre-headless
      openjdk-7-jre-lib
      icedtea-7-plugin 
       
)

